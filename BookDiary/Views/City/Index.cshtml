@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<BookDiary.Models.ViewModels.CityViewModels.CityCreateViewModel>
<h1>See all cities</h1>
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var city in Model)
        {
            <tr>
                <td scope="row">@city.Id</td>
                <td>@city.Name</td>
                <td>
                    <a type="button" class="btn btn-success" asp-action="Edit" asp-controller="City" asp-route-id="@city.Id">Edit</a>
                    <form method="post" asp-controller="City" asp-action="Delete">
                        <button type="submit" class="btn btn-danger" asp-action="Delete" asp-controller="City" asp-route-id="@city.Id">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>